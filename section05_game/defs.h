#ifndef DEFS_H
#define DEFS_H

// Centralized list of parameters

const int SCENE_WIDTH = 800;        // Scene dimensions
const int SCENE_HEIGHT = 800;

const int PLAYER_WIDTH = 20;        // Player dimensions
const int PLAYER_HEIGHT = 20;

const int OBSTACLE_WIDTH = 20;      // Obstacle dimensions
const int OBSTACLE_HEIGHT = 20;

const int W_OBSTACLE_WIDTH = 80;      // Wide Obstacle Dimension
const int W_OBSTACLE_HEIGHT = 20;

const int T_OBSTACLE_WIDTH = 20;      // Tall Obstacle dimensions
const int T_OBSTACLE_HEIGHT = 80;

const int PUCK_WIDTH = 20;          // Puck dimensions
const int PUCK_HEIGHT = 20;

const int FRAME_RATE = 24;          // Time interval between frames in milliseconds

#endif // DEFS_H
